# planner
work planning service

# API

`/workers`
`/workers/<id>`
`/workers/<id>/allocations`
`/allocations`

# Developing

```
$ cd src
$ export FLASK_APP=planner
$ flask run
```

tests

```
$ cd src
$ python -m pytest
```
